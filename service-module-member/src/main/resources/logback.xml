<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<appender name="console" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%date{HH:mm:ss.SSS} [%thread] %-5level %logger{36} ~>: %msg%n</pattern>
		</encoder>
	</appender>

	<appender name="rollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${logback.file.path}/member/member.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${logback.file.path}/member/member.%d{yyyy-MM-dd}.log</fileNamePattern>
		</rollingPolicy>
		<encoder>
			<pattern>%date{HH:mm:ss.SSS} [%thread] %-5level %logger{36} ~>: %msg%n</pattern>
		</encoder>
	</appender>
	
	<!-- project default level -->
	<logger name="com.hxwisec.member" level="${logback.level}" />
	 
	<!-- mybatis log level -->
	<logger name="org.apache.ibatis" level="${logback.level}" />
	<logger name="java.sql" level="${logback.level}" />
	
	<logger name="com.alibaba.dubbo" level="${log.3rd.level}" />
	<logger name="org.springframework" level="${log.3rd.level}" />
	<logger name="org.apache" level="${log.3rd.level}" />
	<logger name="ch.qos.logback" level="${log.3rd.level}" />

	<root level="${logback.level}">
		<appender-ref ref="console" />
		<appender-ref ref="rollingFile" />
	</root>
</configuration>